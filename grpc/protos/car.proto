syntax = "proto3";

option go_package = "./carserver";

package car;

import "google/protobuf/wrappers.proto";

service CarManager {
  rpc GetCarById (google.protobuf.Int32Value) returns (Car) {}
}

message Car {
  int32 id = 1;
  string make = 2;
  string model = 3;
  int32 price = 4;
}